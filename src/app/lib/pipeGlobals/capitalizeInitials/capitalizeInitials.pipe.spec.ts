/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CapitalizeInitialsPipe } from './capitalizeInitials.pipe';

describe('Pipe: CapitalizeInitialse', () => {
  it('create an instance', () => {
    let pipe = new CapitalizeInitialsPipe();
    expect(pipe).toBeTruthy();
  });
});
